apiVersion: v1
kind: Service

metadata:
  name: nats
  namespace: xkcd
  labels:
    app: nats

spec:
  selector:
    app: nats

  ports:
    - name: client
      protocol: TCP
      port: 4222
      targetPort: client
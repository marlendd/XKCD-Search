apiVersion: apps/v1
kind: Deployment

metadata:
  name: search
  namespace: xkcd
  labels:
    app: search

spec:
  replicas: 1

  selector:
    matchLabels:
      app: search

  template:
    metadata:
      labels:
        app: search

    spec:
      containers:
        - name: search
          image: search:local
          imagePullPolicy: IfNotPresent

          ports:
            - name: grpc
              containerPort: 8080
              protocol: TCP

          envFrom:
            - configMapRef:
                name: search-config

          env:
            - name: DB_ADDRESS
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: DB_ADDRESS

          startupProbe:
            tcpSocket:
              port: grpc
            periodSeconds: 2
            failureThreshold: 60

          readinessProbe:
            tcpSocket:
              port: grpc
            periodSeconds: 5
            timeoutSeconds: 1
            failureThreshold: 3

          livenessProbe:
            tcpSocket:
              port: grpc
            periodSeconds: 10
            timeoutSeconds: 1
            failureThreshold: 3

          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
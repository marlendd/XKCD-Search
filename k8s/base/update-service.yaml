apiVersion: v1
kind: Service

metadata:
  name: update
  namespace: xkcd
  labels:
    app: update

spec:
  type: ClusterIP

  selector:
    app: update

  ports:
    - name: grpc
      protocol: TCP
      port: 8080
      targetPort: grpc
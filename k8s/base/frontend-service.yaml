apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: xkcd
  labels:
    app: frontend

spec:
  type: NodePort

  selector:
    app: frontend

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
